interface:
  display_name: "Insurance Agent"
  short_description: "Repo-specific patterns and workflows for insurance-agent"
  default_prompt: "Use the insurance-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true